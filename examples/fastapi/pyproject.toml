[project]
name = "fastapi-example"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "python-dotenv",
    "uvicorn",
    "zuv>=0.0.1",
]

[tool.zuv]
entry = "src/main.py"
volume = "data"
